import { css } from '@emotion/css';

export const imageCardStyles = css({
  width: 200,
  boxShadow: '0px 0px 2px rgba(0, 0, 0, 0.3)',
  position: 'relative',
});

export const imageStyles = css({
  maxWidth: '100%',
  maxHeight: '100%',
});

export const favoriteIconStyles = css({
  position: 'absolute',
  right: 10,
  top: 10,
});
