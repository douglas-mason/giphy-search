import { css } from '@emotion/css';

export const favoriteCategoryStyles = css({
  display: 'flex',
  alignItems: 'flex-start',
});

export const imageContainerStyles = css({
  display: 'flex',
  flexDirection: 'column',
  flexWrap: 'wrap',
});
