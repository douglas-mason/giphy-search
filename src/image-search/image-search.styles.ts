import { css } from '@emotion/css';

export const imageSearchPageStyles = css({
  display: 'flex',
  justifyContent: 'space-between',
  flexWrap: 'wrap',
});

export const imageListContainerStyles = css({
  flexGrow: 2,
});

export const favoriteListContainerStyles = css({});
